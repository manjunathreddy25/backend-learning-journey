using System;
using System.Collections.Generic;

namespace Arrays_and_Collections
{
    class Arrays_and_Collections
    {
        public static void Main(String[] args)
        {
            Storing_Employees.Employees();

            //Arrays.Run();
            //Lists.Run();
            //Dictionarys.Run();
            //Hash_Set.Run();
            //Queues.Run();
            //Stacks.Run();
            //Linked_List.Run();
            //Sorted.Run();
        }
    }
}












/*
Array
 ↓
Fixed size + index

List<T>
 ↓
Dynamic size + index

LinkedList<T>
 ↓
Nodes + links

HashSet<T>
 ↓
Unique values

Queue<T>
 ↓
FIFO

Stack<T>
 ↓
LIFO

Dictionary<K,V>
 ↓
Key → Value

SortedDictionary<K,V>
 ↓
Key → Value + sorted keys

SortedList<K,V>
 ↓
Key → Value + sorted keys + index access
*/